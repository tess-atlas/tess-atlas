name: run toi

on:
  push:
    branches:
      - main  # Change this to the branch you want to trigger the workflow on

jobs:
  build:
    runs-on: nectar

    steps:
    - name: Run My Command
      run: run_toi 103 --quickrun
